#include <cstdint>


class transceiver_t {
public:
    static void initialize() {

    }

    static bool receive(uint8_t* buffer, uint16_t size) {
        
    }

    static void send(uint8_t* buffer, uint16_t size) {

    }

private:

};
